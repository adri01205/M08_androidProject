<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adri.m08_androidproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/footprint"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".controller.service.CronoService" />
        <activity
            android:name=".controller.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.RegisterActivity" />
        <activity android:name=".controller.MenuActivity" />
        <activity android:name=".controller.ChronoActivity" />
        <activity android:name=".controller.HistoryActivity" />
    </application>

</manifest>
